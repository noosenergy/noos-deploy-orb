description: Install locally Python package managers.

steps:
  - run:
      name: Install pipenv/poetry packages.
      command: pip install pipenv==$PIPENV_RELEASE poetry==$POETRY_RELEASE
      environment:
        PIPENV_RELEASE: 2020.11.4
        POETRY_RELEASE: 1.1.4
